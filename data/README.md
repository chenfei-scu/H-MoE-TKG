# Data directory

Place processed datasets under `data/<dataset>/processed/` and semantic caches at `data/<dataset>/semantic_cache.pt`.

Processed split rows use four tab-separated integer columns:

```text
subject_id relation_id object_id timestamp_id
```

Raw ICEWS data are not redistributed in this repository. Obtain them from their cited public sources and retain the original license and terms of use.
